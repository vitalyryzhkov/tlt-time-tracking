# SESSION-HANDOFF — tlt-time-tracking

> Указатель состояния, а не пересказ. Устройство — комментарии в самом
> `index.html` (над `PAYROLL`, над `SCHEDULES_WINTER`, над блоками хранилища),
> «почему» — в сообщениях коммитов.
>
> ⚠️ Репозиторий публикуется через GitHub Pages: этот файл открывается по
> ссылке с сайта. Никаких имён, личных данных и сумм из расчёток сюда.

## Что это

Учёт рабочего времени водителя (автобус и троллейбус, Таллин) с расчётом
зарплаты. Одностраничное приложение: весь код в `index.html`, без сборки и
бэкенда. Данные только в localStorage телефона, ключ `work-tracker-v1`.

Деплой: GitHub Pages из `main`. **Пуш в `main` = прод** примерно через полминуты.

## Открытые вопросы и решения владельца

- **Маршруты с буквой (31A, 8A) и ночные V2, V3, V3A, V6, V7, V8** — в расписание
  не добавлять, пока владелец не скажет (решение 2026-09-14). Поле маршрута
  принимает только цифры.
- **Генератор расписания в репо не лежит** — скрипт жил в скретчпаде сессии.
  При новых нарядах писать заново по правилам ниже или сначала предложить
  положить его в `tools/`. PDF и текст из них в репо не класть: там имена водителей.
- **Летнее расписание не сверено** ни с одним нарядом; границы лета прибиты к
  2026 году (`SUMMER_START`/`SUMMER_END`) — следующим летом приложение останется
  на зимнем наборе. Нужны наряды за летний будний, субботу и воскресенье.
- **Придуманный план 8:00.** Тумблеры, ввод времени и километража создают день с
  `plan: '8:00'`. Отложено: оказалось, владелец сам ставит 8:00 при переводе
  резерва в рабочий, так что вред неясен.
- **Не проверено на реальном iPhone:** скачивание файла копии, загрузка его
  обратно через «Файлы», импорт настоящего текста письма KUUGRAAFIK.
- `lookupShiftTimes` не находит маршрут с ведущим нулём (`'05'` ≠ `'5'`) —
  было и до правок, не чинили.

## Как собирать расписание (`SCHEDULES_WINTER`)

Источник — дневные наряды парков (`*PÄEVAGRAAFIK*`, PDF → текст через `pypdf`):
2. BTO и 3. BTO (автобусы), 4. VKO (троллейбусы). Один наряд на тип дня:
будни одинаковые, суббота и воскресенье — каждый по своему.

Грабли, на которые уже наступили:
- первый блок файла идёт без `Liin:` — номер маршрута отдельной строкой;
- **очередь двумя строками** (по строке на водителя) = смена 1 (ранний кусок)
  + смена 2 (поздний). «Последняя строка побеждает» теряет ранний кусок;
- **служебная метка перед строкой** (`V3 2 4:17 …`, `+48-02 …`) — отбросить метку,
  иначе строка теряется целиком;
- строки без номера очереди (`3:57 … 6:08`) ставить некуда;
- пара «маршрут + очередь» между парками не повторяется, номер маршрута — бывает;
- **сверка не должна делить фильтр строк со сборкой**: брать каждую строку со
  временем под числовым маршрутом и искать её времена в таблице маршрута, не
  разбирая номер очереди. Обе ошибки 2026-09-14 прошли через общий фильтр.

## Журнал

### 2026-09-14 — третий автобусный парк, разорванные очереди (`7f1333f`)

Добавлены наряды 3. BTO за 14/12/13 сентября. Исправлено уже бывшее в проде:
10 очередей, напечатанных двумя строками, лежали с поздним куском в смене 1;
пропадала 44/2 в будни из-за метки `V3` перед строкой.

Review: **failed → исправлено.** Прогон 1 — failed, 1 находка (потеряна 44/2),
исправлено. Прогон 2 — по данным 1197 слотов совпали с нарядами, расхождений 0;
failed за две неточности в комментарии над таблицей — исправлены, эта текстовая
правка повторно не проверялась.
Проверено: построчная сверка 686 строк, 11 подстановок и импорт в браузере,
расчёт зарплаты не изменился.

### 2026-09-05 — Δ на «Итогах» (`e7fb214`)

Δ считается только по дням, где есть и план, и факт; дни без плана названы
строкой под карточкой, тап открывает месяц. Подработка в Δ не идёт.

Review: **not_run.** Проверено вручную в браузере: сумма Δ сходится с Δ по дням,
числительные, переход в месяц, зарплата не изменилась.

### 2026-09-05 — троллейбусные маршруты (`17f9033`)

Добавлены наряды 4. VKO: маршруты 72, 81, 83, 84, 85. Ссылка на расписание для
троллейбусов ведёт на `…-trolleybus`. Ставки у троллейбуса те же, что у
автобуса (подтвердил владелец), названия в интерфейсе не меняли.

Review: **not_run.** Позже нашлась ошибка: разорванные очереди троллейбусов
лежали неверно (см. 2026-09-14). Сверка «621 из 621» в сообщении коммита была
неполной — делила фильтр со сборкой.

### 2026-09-05 — расписание из нарядов 2. BTO (`0cdc02b`)

Зашитая вручную таблица разошлась с нарядами (точно совпадала 141 смена из 346,
расхождения до трёх часов) — пересобрана из нарядов за 7/5/6 сентября.

Review: **not_run.** Позже нашлась ошибка: 32/5 в будни лежала неверно
(см. 2026-09-14).

### 2026-08-29 — тип автобуса, копия данных, праздники

`64a986a` — «Автобус по умолчанию» (обычный / гармошка) с исключениями по дням,
заморозка прошлых дней при смене настройки; выгрузка и восстановление копии
файлом; снимок данных до миграции; защита от затирания нечитаемого хранилища;
праздники Эстонии вычисляются (Пасха), а не берутся из списка; план резервного
дня учитывается; часы на гармошке идут в квартальную переработку.
`8c97b37` — аварийный снимок обновляется при каждом запуске и честно называет
свою дату; экспорт CSV переведён на безопасное скачивание.
`e8ef8b8` — экспорт CSV убран по решению владельца (копия его заменила).

Review для `64a986a`: **failed → исправлено**, девять прогонов; каждый находил
настоящие ошибки, все исправлены. Пуш сделан по слову владельца до возврата
девятого прогона; девятый — failed (устаревший аварийный снимок, небезопасное
скачивание CSV) → исправлено в `8c97b37`, повторно **не проверялось**.
Review для `e8ef8b8`: **not_run** (удаление кнопки и мёртвого кода).
Проверено: расчёт зарплаты сходится с расчёткой владельца за июль до копейки,
круг «копия → очистить → восстановить», порча и переполнение хранилища.
